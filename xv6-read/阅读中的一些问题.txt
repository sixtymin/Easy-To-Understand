
1. fork / exec 两个系统调用即完成新的进程创建
    
    对于这种方式完成进程创建，两个进程内容完全相同，那在父进程中打开的文件句柄，在子进程中也可以共享。    
        1. 那两个进程对文件操作是否需要锁？ 
        2. 两个进程对应的 文件指针是否相同（涉及读写操作）？
    
    文件描述符是指向同一个文件对象，两个进程对同一个文件操作需要锁，并且文件指针是相同的。       
    
2. 